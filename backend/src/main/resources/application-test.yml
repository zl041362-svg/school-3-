spring:
  datasource:
    url: jdbc:h2:mem:zhhs;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
app:
  jwt:
    secret: test-secret-change-me-test-secret-change-me
    expiration-seconds: 3600
